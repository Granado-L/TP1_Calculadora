



/** \brief recibe dos numeros.
 *
 * \param suma los dos numeros.
 *
 * \return devuelve la suma de los dos numeros.
 */
float SumaOperandos(float primerNumero, float segundoNumero);


/** \brief recibe dos numeros.
 *
 * \param resta los dos numeros.
 *
 * \return devuelve la resta de los dos numeros.
 */
float RestaOperandos(float primerNumero, float segundoNumero);


/** \brief recibe dos numeros.
 *
 * \param divide los dos numeros.
 *
 * \return devuelve la divicion de los dos numeros.
 */
float DivisionOperandos(float dividendo, float divisor);


/** \brief recibe dos numeros.
 *
 * \param multiplica dos numeros.
 *
 * \return devuelve la multiplicacion de los dos numeros.
 */
float MultiplicacionOperandos(float primerNumero, float segundoNumero);


/** \brief recibe un numero
 *
 * \param factorea el numero recibido
 *
 * \return devuelve el factorial del numero ingresado.
 */
float FactorialA(float primerNumero);

/** \brief recibe un numero
 *
 * \param factorea el numero recibido
 *
 * \return devuelve el factorial del numero ingresado.
 */
float FactorialB(float segundoNumero);






